main {
	var int x;
  var int y;
  x_1a := (x + y);
  y ( x, y);
  if ((x == y)) {
    y := (x + y);
  };
  while ((x != 100.5)) {
    if ((y < x)){
      y := (y + x);
    };
  };
  do {
    y := (x + y);
  } while((x == y));
  print ((x + y));
  return (x + y);
}
function int teste(){
  var int x;
  do {
    y := (x + y);
  } while((x == y));
}
