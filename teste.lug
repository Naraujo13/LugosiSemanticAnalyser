main {
	var int x;
  var int y;
  x := (x + y);
  y ( x );
  if (( x == y )) {
    y := (x + y);
  };
  while ((x != y)) {
    if ((y < x)){
      y := (y + x);
    };
  };
  do {
    y := (x + y);
  } while((x == y));
  return (x + y);
}
